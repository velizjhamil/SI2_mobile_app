name: forest_microfinance
description: "Forest - Microfinanzas, Inversiones y Servicios Bancarios"
publish_to: 'none'
version: 1.2.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  intl: ^0.19.0
  fl_chart: ^0.68.0
  provider: ^6.1.2
  http: ^1.2.0
  cached_network_image: ^3.3.1
  local_auth: ^2.2.0
  geolocator: ^12.0.0
  camera: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
